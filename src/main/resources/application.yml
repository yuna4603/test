server:
  port: 8888

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/course?serverTimezone=GMT&autoReconnect=true&useSSL=false
    username: root
    password: 123qwe

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/*